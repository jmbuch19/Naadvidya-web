import { MarkdownPage } from '@/components/legal/MarkdownPage';

export const metadata = {
  title: 'Teacher Terms — Naadvidya',
  description: 'Terms that govern teachers who offer classes on Naadvidya.',
};

export default function TeacherTermsPage() {
  return <MarkdownPage slug="teacher-terms" />;
}
