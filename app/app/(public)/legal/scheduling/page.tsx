import { MarkdownPage } from '@/components/legal/MarkdownPage';

export const metadata = {
  title: 'Scheduling & Holiday Policy — Naadvidya',
  description: 'How sessions, holidays, and availability windows work on Naadvidya.',
};

export default function SchedulingPage() {
  return <MarkdownPage slug="scheduling" />;
}
