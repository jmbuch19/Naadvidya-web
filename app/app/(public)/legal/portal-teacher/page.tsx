import { MarkdownPage } from '@/components/legal/MarkdownPage';

export const metadata = {
  title: 'Portal Terms (Teacher) — Naadvidya',
  description: 'Portal usage terms applicable to teachers on Naadvidya.',
};

export default function PortalTeacherPage() {
  return <MarkdownPage slug="portal-teacher" />;
}
