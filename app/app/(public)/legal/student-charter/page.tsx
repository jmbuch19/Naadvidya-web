import { MarkdownPage } from '@/components/legal/MarkdownPage';

export const metadata = {
  title: 'Student Charter — Naadvidya',
  description: 'What students can expect, and what is expected of students, on Naadvidya.',
};

export default function StudentCharterPage() {
  return <MarkdownPage slug="student-charter" />;
}
