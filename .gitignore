# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code — local-only settings (permission allowlists, machine paths).
# .claude/settings.json (shared team config) and .claude/skills/ CAN be committed.
.claude/settings.local.json

# Local env (shouldn't appear at root, but just in case)
.env
.env.local

# Workspace-root naadvidya.env.example currently holds REAL secrets — never commit.
# Restore it as a template (delete real values) before removing this line.
naadvidya.env.example
naadvidya.env
